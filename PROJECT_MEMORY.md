# PROJECT_MEMORY

updated: 2026-06-09

## Project State

- 本目录是 `macro_dashboard` 的公开 Pages 发布仓库本地副本。
- 这里通常只保留静态站产物，不承载 ETL、源码开发或自动化调度逻辑。
- 发布流程和长期问题记录以 `../macro_dashboard/PROJECT_MEMORY.md` 为准。

## Maintenance Notes

- 默认不手工编辑 `index.html`、页面目录、`_astro/` 或 `data/`。
- 如需修复页面或数据，应回到 `../macro_dashboard` 修改源码、运行 ETL/校验/构建，再同步发布。
- 只有排查 Pages 产物、回滚发布或验证同步结果时，才直接检查本目录。
